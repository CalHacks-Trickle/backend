
// This Map will store the live application status for each user.
// Key: user email (e.g., 'test-user@trickle.app')
// Value: current application name (e.g., 'Visual Studio Code')
const liveApps = new Map();

module.exports = liveApps;
